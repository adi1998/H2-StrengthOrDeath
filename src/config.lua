local config =
{
    allow_both = true,
}

local configDesc =
{
    allow_both = "Allow the card not forced to appear in the random arcana selection. This is always `true` if Flip the Arcana is also installed"
}

return config, configDesc